{
  "name": "oikonomia",
  "version": "0.1.0",
  "private": true,
  "description": "AI-assisted household financial stewardship and budgeting, built for households in India",
  "license": "MIT",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/check.mjs",
    "deploy": "npm run build && wrangler pages deploy public --project-name oikonomia --branch main"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
